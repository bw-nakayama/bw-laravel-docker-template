# Laravel Lesson レビュー②

## Todo編集機能

### @method('PUT')を記述した行に何が出力されているか
<input type="hidden" name="_method" value="PUT">

### findメソッドの引数に指定しているIDは何のIDか
取得したいTodoのid

### findメソッドで実行しているSQLは何か
SELECT * FROM people WHERE id = $id

### findメソッドで取得できる値は何か
1つのレコード　Todoモデルのインスタンス

### saveメソッドは何を基準にINSERTとUPDATEを切り替えているのか
newで生成したインスタンスか、Findメソッドなどを使用しデータベースから取ってきたものか

## Todo論理削除

### traitとclassの違いとは
traitは単体ではインスタンス化できずクラス内に記述することでtrait内に記述されたメソッドなどを使用できるようにするもの

### traitを使用するメリットとは
複数の（継承などをしていない）クラスにおいて同じメソッドを使いまわすことができる

## その他

### TodoControllerクラスのコンストラクタはどのタイミングで実行されるか
リクエストが発生した後ルートによってどのコントローラーを使用するかが決められ、その直後にインスタンス化。
処理後には破棄されているらしい

### RequestクラスからFormRequestクラスに変更した理由
ただのRequestクラスではバリデーションが行えないため

### $errorsのhasメソッドの引数・返り値は何か
連想配列のキー　そのキーに対応する値が存在するかどうかをboolで返す

### $errorsのfirstメソッドの引数・返り値は何か
連想配列のキー　キーに対応する最初の値

### フレームワークとは何か
枠組み。開発しやすくするためのもの。　ディレクトリ構成などが決まっていることにより作りやすいし分かりやすい

### MVCはどういったアーキテクチャか
モデル・ビュー・コントローラの三つに分かれている
DBとのやりとりを主に行うモデル、ユーザインタフェース部分を司るビュー、入力データに基づきモデルとビューの制御を担うコントローラ

### ORMとは何か、またLaravelが使用しているORMは何か
ORMはオブジェクト指向プログラム言語とデータベースの間をうまく取り持ってくれる技術
オブジェクト指向プログラミング言語でデータベースを簡単に操作できる
Eloquentというものが使われている

### composer.json, composer.lockとは何か
どちらもPHPの依存するライブラリ・バージョン管理をするためのもの
初回はcomposer.jsonに記述したものをDLするが2回目からはcomposer.lockが使用される
これを使用することでチーム間のバージョンを一致させることが容易になる

### composerでインストールしたパッケージ（ライブラリ）はどのディレクトリに格納されるのか
vendorディレクトリ